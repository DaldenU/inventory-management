{
  "homepage": "https://DaldenU.github.io/inventory-management",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "scripts": {
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
